\documentclass[11pt]{ctexart}
\input{preamble.tex}

% ============================================================
% 带读手册模板 —— 使用说明（生成时删除本注释块）
% 1. 标题、作者信息按论文实际情况替换。
% 2. 每个 \section / \subsection 都必须写满，不能留空占位。
% 3. 前置知识地图：只列这篇论文真正用到的背景知识，按"流水线顺序"排列，
%    每条都要用 term 环境（见 preamble.tex）并说明"论文里具体怎么用它"。
% 4. 术语表：用 longtable，英文术语/符号 + 中文含义，覆盖论文里第一次出现的
%    所有缩写、自定义符号（例如变换矩阵、下标记号约定）。
% 5. 逐段精读：按论文原始章节顺序，一节不漏；每一段落/公式都要解释"是什么、
%    为什么这么设计、和前后文的关系"，避免只复述原文。公式讲解要点出物理/
%    工程直觉，不能只抄公式。
% 6. 每个较长小节结束后，用 tip 环境给一个"读法建议"或"如何理解"的小结，
%    做到"循循善诱"——多用启发式提问（"你可能会问……""不妨把……想象成……"）。
% 7. 结尾必须有：(a) 自测问题列表 (b) 写作层面的启发（这篇论文的结构/论证
%    套路对读者自己写论文有什么可借鉴之处）。
% ============================================================

\title{\bfseries 带读手册\\[4pt]\large \textit{<PAPER TITLE>}\\[2pt]\normalsize （<中文标题>）}
\author{整理：Claude Code\ \ |\ \ 供 <课题组/用户> 文献研读使用}
\date{\today}

\begin{document}
\maketitle
\tableofcontents
\newpage

\section{文章速览}
% 用 tip 环境给出：标题、作者、单位、方向/发表会议；然后一段"一句话概括"，
% 一段"核心创新点用大白话讲一遍"，并提示读者接下来先看前置知识。

\section{读前必备：前置知识地图}\label{sec:prereq}
% 先给出"流水线图"（用 -> 串联这篇论文的处理流程），
% 再逐个 term 环境讲解流水线上每一步需要的背景知识。

\section{术语与符号对照表}
% longtable：术语/符号 | 含义

\section{逐段精读}
% 按论文实际章节结构展开，每个 \subsection 对应原文一个大节。
% 摘要按"问题 -> 现有工作局限 -> 本文思路 -> 验证方式"四段式拆解。
% 方法章节：算法伪代码/公式要逐行/逐式解释，公式用 align/equation 原样保留符号。
% 实验章节：逐个实验设置说明"验证了什么"，结果表格要点出关键数字对比。
% 结论：作为"复习提纲"呼应引言的贡献列表。

\section{读完之后，你应该能回答这些问题（自测）}
% 6-10 道问题，覆盖前置知识、核心设计动机、公式细节、实验结论、局限与未来工作。

\begin{tip}[写作层面的启发]
% 提炼 3-5 条这篇论文在结构/论证套路上值得模仿的地方。
\end{tip}

\end{document}
